# OpenClaw Kermit – Config Sync

This repo stores **redacted** OpenClaw configuration and behavior files for review and version tracking.

## What’s included
- `config/openclaw.json` (secrets removed)
- Behavior/identity files: `SOUL.md`, `IDENTITY.md`, `USER.md`, `AGENTS.md`, `BOOTSTRAP.md`, `HEARTBEAT.md`, `TOOLS.md`
- Daily memory logs under `memory/`
- Cron store under `cron/` (if present)

## Update policy
I will update this README **every time I sync files** to include a brief summary and timestamp.

---

### Update Log
- **2026-02-07**: Initial sync of redacted config + behavior files.
